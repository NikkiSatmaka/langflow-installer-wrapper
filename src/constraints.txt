# Pins known-breaking transitive dependencies. Currently empty: every
# transitive dependency ships pre-built wheels on all target platforms
# (win-amd64, macos-arm64, linux-x64). Add upper bounds here if a future
# dependency ships a source-only release without wheels.
